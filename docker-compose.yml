version: '3.8'

services:
  pardos-frontend:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src
      - ./public:/app/public
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true
